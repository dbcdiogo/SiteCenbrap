@model Biblioteca.Entidades.Paginas

<div class="container margin-top-50">
    <div class="row">
        @{
            ViewBag.Title = Model.titulo;
        }

        <h1>@Model.titulo</h1>

        <div class="text-center margin-top-50"><img src="~/Images/latam.jpeg" /></div>

    </div>
</div>
